// Centralised project-wide constants
/**
 * Grammar rule keys that the UI should treat as lock-toggle-able in Advanced Options.
 * These are common across bundled generators and centralised here so both engine
 * and UI can stay in sync.
 */
export const LOCKABLE_FIELDS = [
  'preacher_name',
  'platforms',
  'mediaContexts',
  'divine_title'
];
